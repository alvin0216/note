{
  "name": "alvin-note",
  "version": "1.0.0",
  "author": "reco_luan",
  "scripts": {
    "dev": "vuepress dev . --open --host \"localhost\" --cache .cache",
    "build": "vuepress build .",
    "build:gh-pages": "cross-env TARGET=gh-pages vuepress build .",
    "push": "node ./scripts/push.js",
    "cli": "node ./scripts/md-cli.js"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "vuepress": "1.8.2",
    "vuepress-theme-reco": "1.6.11"
  },
  "description": "前端开发",
  "dependencies": {
    "vuepress-plugin-demo-container": "^0.2.0",
    "vuepress-plugin-element-tabs": "^0.2.8"
  }
}

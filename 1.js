/**
 * @param {number[]} numbers
 * @return {number}
 */
var minArray = function (numbers) {
  let l = 0,
    r = numbers.length;
  while (l < r) {
    // l < r 正常二分  r = mid
    // [4 5 1 2 ]
    // l > r
    //    mid
  }
};

minArray([3, 4, 5, 1, 2]);
minArray([2, 2, 2, 0, 1]);
